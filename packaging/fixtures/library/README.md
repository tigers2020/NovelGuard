# Smoke fixture library (PR-24)

Small text library for **manual packaged smoke only**.

- Do not point production libraries here.
- Destructive finalize/cleanup/move tests must use this folder only.
- Record results in `docs/release/smoke-record-template.md`.
