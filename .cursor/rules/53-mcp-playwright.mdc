---
description: Playwright MCP for browser and UI flow verification
alwaysApply: false
---

# Playwright verification

Use when the task requires **observed** UI behavior (regression, new flow, layout).

Workflow: navigate → snapshot → act → re-snapshot. Do not claim UI fixed without evidence.

Skip for backend-only or static markup tweaks with no behavior change.
